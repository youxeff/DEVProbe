APP_ENV=development
# Local quickstart. Docker uses PostgreSQL; production must use PostgreSQL.
DATABASE_URL=sqlite:///./devprobe.db
# Optional for public repositories. Required for private repositories.
GITHUB_TOKEN=
GITHUB_TIMEOUT_SECONDS=10
GITHUB_MAX_PAGES=30
GITHUB_MAX_RESPONSE_BYTES=8388608
